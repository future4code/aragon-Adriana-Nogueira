<!-- let atividadesTerminadas = tarefas.filter((tarefa.status)=> {
return resultado.status.includes("done")

})
const atividadeFinal = atividadesTerminadas.map((done))=>{
return done.titulo
})
retunr atividadeFinal
} -->